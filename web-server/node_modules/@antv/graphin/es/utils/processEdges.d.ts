import { IUserEdge } from '../index';
/**
 *
 * @param edges 边的集合
 * @param {poly,loop} 设置多边和自环多边的distance
 */
declare const processEdges: (edges: IUserEdge[], { poly, loop, loopLabelPosition, }?: {
    /** poly distance */
    poly?: number | undefined;
    /** loop distance */
    loop?: number | undefined;
    /** loop label position based on loop edge */
    loopLabelPosition?: number | undefined;
}) => IUserEdge[];
export default processEdges;
