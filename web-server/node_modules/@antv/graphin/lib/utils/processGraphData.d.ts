/**
 * 根据key取对象的值
 * @param obj 对象
 * @param keyString 对象字段,支持多层,例如data.property.key
 * @returns
 */
export declare const getEnumValue: (obj: any, keyString: string) => string;
/**
 * 根据字段分类，得到分类后的枚举值Map
 * @param data nodes or edges
 * @param sortKey 分类的字段，例如 nodeType 或者 data.type
 * @returns
 */
export declare const getEnumDataMap: (data: any[], sortKey: string) => Map<string | number, any[]>;
