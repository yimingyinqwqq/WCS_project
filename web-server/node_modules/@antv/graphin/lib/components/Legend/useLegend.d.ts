import type { LegendProps } from './typing';
declare const useLegend: ({ bindType, sortKey, }: {
    bindType: LegendProps['bindType'];
    sortKey: LegendProps['sortKey'];
}) => {
    dataMap: Map<any, any>;
    options: {};
} | {
    dataMap: Map<string | number, any[]>;
    options: {
        /** 颜色 */
        color: string;
        /** 值 */
        value: string | number;
        /** 标签 */
        label: string | number;
        /** 是否选中 */
        checked: boolean;
    }[];
};
export default useLegend;
