import React from 'react';
import { IG6GraphEvent } from '../../index';
export interface State {
    /** 当前状态 */
    visible: boolean;
    x: number;
    y: number;
    /** 触发的元素 */
    item?: IG6GraphEvent['item'];
}
export interface Props {
    bindType: 'node' | 'edge';
    container: React.RefObject<HTMLDivElement>;
}
declare const useTooltip: (props: Props) => {
    /** 当前状态 */
    visible: boolean;
    x: number;
    y: number;
    /** 触发的元素 */
    item?: import("@antv/g6-core").Item | null | undefined;
};
export default useTooltip;
