/* eslint-disable @typescript-eslint/no-explicit-any */
import { GraphinContext } from '@antv/graphin';
import * as React from 'react';
import "./index.css";

var Node = function Node(props) {
  var children = props.children;

  var _React$useContext = React.useContext(GraphinContext),
      tooltip = _React$useContext.tooltip;

  var context = tooltip.node;
  var item = context.item;

  if (typeof children !== 'function') {
    console.error('<Tooltip.Node /> children should be a function');
    return null;
  }

  return React.createElement("div", {
    className: "graphin-components-tooltip-content"
  }, children(item.getModel()));
};

export default Node;